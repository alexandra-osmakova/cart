import React from "react";

interface IProps {    }

const Header: React.FC<IProps> = (props) => {

    return (
        <header>
            Header
        </header>
    );
};

export default Header;